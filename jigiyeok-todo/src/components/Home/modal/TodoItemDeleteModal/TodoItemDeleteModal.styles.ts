import Typography from "@/components/common/Typography";
import styled from "styled-components";

export const DeleteModalSubText = styled(Typography).attrs({
  variant: "subtitle2",
})`
  text-align: center;
`;
